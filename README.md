# Routes
A multi-agent AI system that reasons about errand order, not just the route between stops. Built with LangGraph + Gemini — parses natural language, enriches with real-world data, infers dependencies, and explains every decision.
